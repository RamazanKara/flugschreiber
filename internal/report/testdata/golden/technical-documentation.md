# Technical documentation: Support-Assistent

Generated by Flugschreiber v0.1.0-test on 2026-05-04T09:00:00Z from observed traffic in `/var/lib/flugschreiber`.

> **This is a skeleton, not a compliance document.** Sections marked *observed* are filled
> in from evidence Flugschreiber actually recorded. Everything marked **TODO** requires a
> human who understands the system and its use case. Flugschreiber has no way to know
> those things and does not guess at them.
>
> This document is not legal advice. An LLM is not high-risk in itself. Obligations under the
> EU AI Act attach to the *use case*, not to the technology. Whether Annex III applies to
> this system is a determination you have to make; this document is shaped like Annex IV so
> that it is useful if it does, and harmless if it does not.

## Document control

| Field | Value |
| --- | --- |
| System name | Support-Assistent |
| Organisation | Muster GmbH |
| Role under the AI Act | deployer |
| Environment | production |
| Contact point | ai-governance@muster.example |
| Evidence window | 2026-05-04T08:31:00Z to 2026-05-04T08:37:00Z |
| Records covered | 7 (5 inference events) |
| Integrity | hash chain verified intact |

## 1. General description of the AI system

### 1.1 Intended purpose

drafting first-line support replies, reviewed by an agent before sending

> **TODO:** State explicitly what the system is *not* intended for, and any use you have decided to exclude. Reasonably foreseeable misuse belongs here too.

### 1.2 Models and versions observed

*Observed.* Over the evidence window, the following model identifiers were requested by
callers and reported as served by the upstream:

| Model requested | Model served | Requests | Streamed | Errors | Prompt tokens | Completion tokens |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| `llama-3.1-8b-instruct` | `llama-3.1-8b-instruct` | 2 | 1 | 0 | 390 | 404 |
| `bge-m3` | `bge-m3` | 1 | 0 | 0 | 96 | 0 |
| `llama-3.1-8b-instruct` | `llama-3.1-8b-instruct-awq` | 1 | 0 | 0 | 340 | 28 |
| `mistral-7b` | `n/a` | 1 | 0 | 1 | 0 | 0 |

In at least one case the served identifier differs from the requested one: the upstream
substituted a model. That substitution belongs in this document, with the reason for it.

> **TODO:** For each model above: record its origin (vendor, open-weights repository, internal fine-tune), the version or digest pinned in your deployment, and the licence it is used under. Flugschreiber sees the identifier the API reports, which is not always the same as the artefact you deployed.

### 1.3 System architecture and interfaces

*Observed.* Callers reached the following API endpoints through Flugschreiber:

| Endpoint | Requests |
| --- | ---: |
| `/v1/chat/completions` | 4 |
| `/v1/embeddings` | 1 |

Traffic was forwarded to: http://vllm:8000 (5).

20% of inference requests used server-sent-event streaming.
Streamed responses are reassembled and recorded as the complete message the caller
received.

> **TODO:** Describe what sits in front of the application and what sits behind the model: authentication, retrieval sources, tool integrations, downstream systems that consume the output, and any human review step. Flugschreiber only sees the model API boundary.

### 1.4 Form of deployment and hardware

> **TODO:** State how the system is deployed (container, Kubernetes, VM), on what hardware it runs, and in which jurisdiction that hardware sits. If inference runs on infrastructure you do not control, say so and name the operator.

## 2. Elements of the system and the development process

### 2.1 Generation parameters in use

*Observed.* Callers set the following generation parameters:

| Parameter | Range observed | Requests |
| --- | --- | ---: |
| `max_tokens` | 256 to 1024 | 3 |
| `temperature` | 0 to 0.7 | 3 |
| `top_p` | 0.95 | 1 |

### 2.2 Tools and function calling

*Observed.*

Tools offered to the model: issue_refund, lookup_order.

Tools the model actually called: lookup_order (1).

> **TODO:** For every tool above, document what it can do, what it can reach, and whether its effects are reversible. A model that can call a tool that writes to a production system is a materially different risk picture from one that can only read.

### 2.3 Data used by the system

> **TODO:** Describe the data the system processes at inference time: what users submit, what is retrieved and injected as context, and what leaves the boundary. If the system was fine-tuned or is used with a retrieval corpus, describe those datasets, their provenance, and their licensing. Flugschreiber records traffic, not training data, so this section cannot be generated.

> **TODO:** Record whether personal data is processed, the legal basis under GDPR, and the link to the DPIA if one exists. See section 3.2 for what Flugschreiber itself retains.

### 2.4 Development process and validation

> **TODO:** Describe how the system was built and how changes to it are reviewed and released. Reference your repository, CI pipeline and release process rather than restating them.

> **TODO:** Describe the pre-deployment testing that was performed and what its results were.

## 3. Monitoring, functioning and control

### 3.1 Logging (Article 12 and Article 19)

*Observed.* Flugschreiber records one structured, hash-chained event per model
interaction. Over the evidence window it captured:

| Measure | Value |
| --- | --- |
| Total records | 7 |
| Inference events | 5 |
| Distinct calling identities | 3 (recorded as salted hashes, not credentials) |
| Distinct sessions | 3 |
| Requests with errors | 1 (20%) |
| Prompt tokens | 826 |
| Completion tokens | 432 |
| Median end-to-end latency | 812.4 ms |
| 95th percentile latency | 2240.9 ms |
| Event types recorded | inference (5), human_intervention (1), incident (1) |
| HTTP status codes | 200 (4), 503 (1) |
| Finish reasons | stop (2), tool_calls (1) |

Each record carries: an RFC3339 timestamp, a monotonic sequence number, request and
session identifiers, the requested and served model, the endpoint and upstream, generation
parameters, token usage, latency, HTTP status, a salted hash of the calling identity, and
the content record described in 3.2.

> **TODO:** Article 12 asks for logs appropriate to the intended purpose. Confirm that the fields above are sufficient for your use case, and record here anything you log elsewhere that Flugschreiber does not see, for example application-level user identity, or the retrieval documents injected into a prompt.

### 3.2 What is recorded of the content itself

*Observed.* The active content mode is **`hash`**.
Modes seen across the recorded window: hash (5).

- `hash`: a SHA-256 of the exact request and response bytes is recorded; no prompt or
  completion text is retained. This is the default, chosen for GDPR Article 5(1)(c) data
  minimisation: a proxy that stores every prompt by default would create a new copy of
  whatever users type, in a system that was not designed to hold it.
- `redact`: text is retained with configured patterns replaced.

  Pattern-based redaction is best-effort by nature; free text can carry personal data in
  forms no regular expression will match.
- `store`: full request and response text is retained.

In every mode the SHA-256 is computed over the complete bytes that crossed the wire. That
is what lets a transcript held elsewhere be proven to be the transcript of the interaction
this log attests to, even when the log itself holds no text.

> **TODO:** Record why the chosen content mode is appropriate here, and where the corresponding retention and access controls are documented.

### 3.3 Retention

*Observed.* Configured retention: **180 days** (~6.0 months).
Evidence on disk: **5.4 KiB (5523 bytes)**.

Article 19 expects automatically generated logs to be kept for at least six months where
they are under the provider's control, subject to other applicable law. Flugschreiber
refuses to start with a retention setting below 180 days.

Enforcement is `flugschreiber retention --enforce --confirm`, which deletes whole
segments only, oldest first, and only when every record in them is beyond the
retention period. A `LEGAL_HOLD` file in the evidence directory blocks all deletion
while it exists. Nothing deletes evidence without those two explicit flags, whether
typed by a person or scheduled as a job.

> **TODO:** Record where the evidence directory is stored, who can read it, how it is backed up, and who or what runs retention enforcement on what schedule. If nothing runs it, records past the retention period accumulate until something does, which is a data-minimisation question, not a tooling one.

### 3.4 Integrity mechanism

*Observed.* Every record carries the SHA-256 of its predecessor and a SHA-256 of its own
contents, forming a chain. Altering, inserting or removing any record breaks the chain at
that point and at every point after it.

| Measure | Value |
| --- | --- |
| Verification result | **intact**, every record hash and link verified |
| Segments | 1 (seg-00000001.jsonl) |
| Chain head hash | `3e71b916cb2ea651eae4e13a9f696884ca979c65c53dea8915a5f710b0aca36f` |
| Signed checkpoints | 1, of which 1 verified against both the signature and the chain |
| Attestation | **attested**: at least one Ed25519 checkpoint signature verified and matches the chain |
| Signing key id | `9a82517f9af19416` |
| Time anchoring | none; checkpoint times are this host's clock |
| Verified at | 2026-05-04T09:00:00Z |

Anyone can re-run this check against the evidence directory with:

```
flugschreiber verify --dir <evidence-directory>
```

The verifier reads only the files on disk. It needs no running server and no access to
your infrastructure, so the check can be repeated by an auditor holding nothing but a copy
of the log.

The chain plus the verified checkpoints mean this log cannot have been rewritten by
anyone who did not also hold the signing key. What remains open is the key's custody,
which is the security boundary.

> **TODO:** Record what protects the evidence directory and the signing key: append-only or object-lock storage, off-host replication of sealed segments, restricted filesystem permissions, and where the key lives relative to the evidence. A key kept beside the log protects against much less than a key kept elsewhere.

### 3.5 Human oversight

*Observed.* 1 human intervention record(s) were written to the
evidence chain during the window: override (1).
Each carries the actor, the decision, and the interaction it concerns, in the same
tamper-evident chain as the interactions themselves.

> **TODO:** Describe the oversight process those records come from: who the actors are, what authority they have, and what triggers a review. Recorded interventions evidence that oversight happened; they do not describe how it is organised.

### 3.6 Accuracy, robustness and cybersecurity

> **TODO:** Record the accuracy levels the system is expected to achieve and how they are measured. Record what happens when the upstream is unavailable or returns an error. The error rate observed above is a starting point for that discussion.

> **TODO:** Record the security controls around the model endpoint: authentication, network isolation, rate limiting, and how prompt-injection risk is handled if the system processes untrusted input.

## 4. Appropriateness of the performance metrics

> **TODO:** State which metrics you use to judge whether this system is working, and why those metrics are the right ones for the intended purpose. Flugschreiber measures traffic, latency and token usage; none of those tell you whether the output was any good.

## 5. Risk management system (Article 9)

> **TODO:** Reference your risk management process: the risks identified for this use case, the measures taken, and the residual risk accepted. Flugschreiber deliberately does not generate this, because a generated risk assessment would be worse than none, because it would look like one.

## 6. Changes made through the lifecycle

> **TODO:** Record substantial changes to the system and when they were made. Model version changes are visible in section 1.2 when they appear in traffic; changes to prompts, retrieval sources, tools and thresholds are not visible to Flugschreiber and need recording here.

## 7. Harmonised standards applied

> **TODO:** List any harmonised standards or common specifications applied, in full or in part. If none, say so explicitly.

## 8. EU declaration of conformity

> **TODO:** Attach the declaration if one is required for this system. If the system is not high-risk and no declaration is required, state that and record the reasoning that led to that conclusion. The reasoning is the valuable part.

## 9. Post-market monitoring and serious incidents (Article 73)

*Observed.* 1 incident record(s) were written to the evidence chain during
the window: serious (1). Each carries the
reporter, the severity, and the interaction it concerns, in the same tamper-evident chain
as the interactions themselves.

> **TODO:** For any serious incident above, record the Article 73 reporting decision: whether it was reportable, to which authority, and when. A recorded incident is evidence that something was noticed; it is not the report itself.

## Appendix A: regulatory timeline

Dates reflect the timeline following the Digital Omnibus agreement. Verify against the
current text before relying on them.

| Date | What applies |
| --- | --- |
| 2 August 2026 | Article 50 transparency obligations |
| 2 December 2026 | New prohibitions |
| 2 December 2027 | Annex III high-risk obligations |
| 2 August 2028 | Annex I obligations |

Within the first row, one duty starts later: the machine-readable marking in
Article 50(2) applies from 2 December 2026 for systems already on the market on
2 August 2026. The interaction disclosure in Article 50(1) is not deferred.

## Appendix B: how this document was produced

Flugschreiber read 7 records from `/var/lib/flugschreiber`, covering
2026-05-04T08:31:00Z to 2026-05-04T08:37:00Z, verified the hash chain, and filled in every section it could support
with observed evidence. Sections it could not support are marked **TODO** rather than
guessed at.

Field-by-field mapping of the log schema to the provisions it supports is in `MAPPING.md`
in the Flugschreiber repository, including its caveats.
